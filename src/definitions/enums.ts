export enum APIMethod {
  DELETE = 'DELETE',
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT'
}

export enum LoggerLevel {
  DEBUG = 'DEBUG',
  ERROR = 'ERROR',
  INFO = 'INFO',
  WARN = 'WARN'
}

export enum WriteMode {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE'
}
