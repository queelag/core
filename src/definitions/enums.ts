export enum APIMethod {
  DELETE = 'DELETE',
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT'
}

export enum BooleanValue {
  FALSE = 'FALSE',
  TRUE = 'TRUE'
}

export enum LoggerLevel {
  DEBUG = 0,
  INFO = 1,
  WARN = 2,
  ERROR = 3
}

export enum WriteMode {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE'
}
