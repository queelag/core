import { describe, it } from 'vitest'

describe('ImageUtils', () => {
  it('gets the base64 of an image element', () => {
    // mock canvas
  })

  it('preloads a set of images', () => {
    // mock img element and canvas
  })
})
