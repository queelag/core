{
  "author": "Dario Sechi",
  "dependencies": {
    "axios": "^0.21.1",
    "js-cookie": "^2.2.1",
    "nanoid": "^3.1.23"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.17.1",
    "@rollup/plugin-typescript": "^8.2.1",
    "@types/axios": "^0.14.0",
    "@types/jest": "^26.0.22",
    "@types/js-cookie": "^2.2.6",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "rollup": "^2.52.7",
    "rollup-plugin-terser": "^7.0.2",
    "ts-jest": "^27.0.3",
    "tslib": "^2.3.0",
    "typescript": "4.3.5"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "@queelag/core",
  "scripts": {
    "test": "yarn jest --coverage",
    "build": "rm -rf declarations && rm -rf dist && yarn tsc && yarn api-extractor run --local && yarn rollup -c rollup.config.js",
    "tarball": "yarn version && cp package.json dist/. && cd dist && yarn pack",
    "deploy": "yarn test && yarn build && yarn tarball && yarn publish dist/*.tgz --access public"
  },
  "version": "1.0.0"
}
